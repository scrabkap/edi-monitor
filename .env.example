# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# Deployment Configuration
STACK_NAME=edi-monitor-stack
ENVIRONMENT=dev
S3_BUCKET=edi-monitor

# API Configuration (Auto-populated after deployment)
API_ENDPOINT=https://your-api-id.execute-api.us-east-1.amazonaws.com/dev

# Frontend Configuration
FRONTEND_URL=https://your-cloudfront-url.cloudfront.net
